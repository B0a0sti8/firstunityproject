using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StorageChestCanvasScript : InventoryBagScript
{
    public override void Awake()
    {
        AddSlots(60);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
